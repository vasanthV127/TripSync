# robots.txt for TripSync
# Allow all search engines to crawl the site

User-agent: *
Allow: /

# Disallow admin-only pages from search engines
Disallow: /admin/
Disallow: /api/

# Sitemap location
Sitemap: https://trip-sync-admin.vercel.app/sitemap.xml

# Crawl-delay (optional, helps prevent server overload)
Crawl-delay: 1
